<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        {% render 'partials/components/head_meta.liquid', siteSettings: siteSettings, eleventy: eleventy %}
        {% render 'partials/components/head_assets.liquid' %}
    </head>
    <body>
        {% block main %}
        {% endblock %}
    </body>
</html>
